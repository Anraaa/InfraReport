{
    "version": 2,
    "routes": [
        {
            "src": "/(css|js|images|assets|favicon.ico|robots.txt|build/.*|storage/.*|vendor/.*)",
            "dest": "/public/$0"
        },
        {
            "handle": "filesystem"
        },
        {
            "src": "/.*",
            "dest": "/public/index.php"
        }
    ]
}